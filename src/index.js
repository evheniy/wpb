import history from './history';
import {
  store,
  injectReducer,
  injectEpic,
  inject,
} from './store';
import app from './app';
import router from './router';

export {
  history,
  store,
  injectReducer,
  injectEpic,
  inject,
  app,
  router,
};
